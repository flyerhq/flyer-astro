---
import { cn } from "@helpers/utils";

interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---

<kbd
  class={cn(
    "bg-surface border border-divider rounded-sm text-dark-grey justify-center items-center text-center h-5 w-5 text-[13px] font-mono leading-[18px]",
    className,
  )}
>
  <slot />
</kbd>
